import { useState, useEffect } from "react";

export default function AudioPlayer(props) {
    const {playingPodcast, setPlayingPodcast} = props

    return (
        <div className="audioplayer--container">
            
        </div>
    )
}